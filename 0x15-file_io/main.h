#ifndef MAIN_H
#define MAIN_H

/* Header files includes */
#include <stdio.h>
#include <unistd.h>
#include <fcntl.h>

/* Functions definition */
int _putchar(char c);
ssize_t read_textfile(const char *filename, size_t letters);

#endif
